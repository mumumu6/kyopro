#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
int main()
{
    int n, m;
    cin >> n >> m;
    int ans = 0;
vector<int> c(m);
vector
    rep(i, m)
    {
        int a, b;
        cin >> a >> b >> c(i);
        
    }
    cout << ans << endl;
}